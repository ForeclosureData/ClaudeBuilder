// Overwritten by netlify.toml's build command with the captured stdout/stderr
// of `prisma db push` from the most recent production build -- diagnostic
// only, see apps/web/app/api/internal/db-push-log/route.ts.
export const dbPushLog = "no build has generated this file yet (local placeholder)";

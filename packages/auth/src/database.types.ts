/**
 * Placeholder for Supabase's generated database types
 * (`supabase gen types typescript`). Kept as a loose type for now so
 * `createClient<Database>()` calls compile without requiring a live
 * Supabase project during local development; regenerate against a real
 * project before relying on generated table types for compile-time safety.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
